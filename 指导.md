# 章幕引擎配置与使用指导

本文档用于指导开发者从零配置并运行章幕引擎。

## 1. 准备环境

1. 安装微信开发者工具。
2. 克隆项目仓库。
3. 使用微信开发者工具导入项目。
4. 小程序源码目录选择 `miniprogram/`。
5. 云函数目录选择 `cloudfunctions/`。

## 2. 开通云开发

1. 在微信开发者工具中打开“云开发”。
2. 创建云开发环境。
3. 在 `miniprogram/app.js` 中按需填写云环境 ID；留空时使用默认环境。

## 3. 创建数据库集合

在云开发数据库中创建：

```text
projects
chapters
extracted_infos
episodes
scripts
script_versions
generation_logs
```

## 4. 配置模型环境变量

进入云开发控制台，给需要调用模型的云函数配置环境变量。

DeepSeek：

```bash
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=your_api_key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
```

Gemini：

```bash
LLM_PROVIDER=gemini
GEMINI_API_KEY=your_api_key
GEMINI_MODEL=gemini-2.5-flash
```

OpenAI：

```bash
LLM_PROVIDER=openai
OPENAI_API_KEY=your_api_key
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4.1-mini
```

## 5. 部署云函数

在微信开发者工具中逐个右键以下云函数目录，选择“上传并部署：云端安装依赖”：

```text
initProject
parseNovel
uploadNovelFile
extractInfo
planEpisodes
splitScenes
generateScript
validateYaml
repairYaml
polishDialogue
saveVersion
exportScript
```

## 6. 使用流程

1. 打开首页。
2. 点击“新建改编项目”，填写项目名称、小说标题、作者、题材和目标时长。
3. 点击“导入小说文本”，粘贴 3 章以上小说，或上传 TXT、Markdown、DOCX。
4. 在章节检查页确认章节数量、标题和字数。
5. 点击进入 AI 生成页，生成剧本 YAML。
6. 在剧本预览页查看人物、地点、分集和场景。
7. 进入场景编辑页调整场景内容和 beats。
8. 进入 YAML 编辑页执行校验、修复和保存。
9. 在版本记录页恢复历史版本。
10. 在剧本预览页导出 YAML、Markdown 或 TXT。

## 7. 示例文件

输入示例：

```text
examples/sample_novel_3chapters.txt
```

输出示例：

```text
examples/sample_output.yaml
examples/sample_output.txt
```

## 8. 常见问题

### 小程序能打开但云函数失败

检查云开发环境是否开通、云函数是否部署、数据库集合是否创建。

### AI 生成使用了规则化结果

检查云函数环境变量中的 API Key 和 `LLM_PROVIDER` 是否正确。

### DOCX 解析失败

确认 `uploadNovelFile` 云函数部署时选择了云端安装依赖，确保 `mammoth` 已安装。

### YAML 校验失败

进入 YAML 编辑页查看错误列表，点击 AI 修复后重新校验。
